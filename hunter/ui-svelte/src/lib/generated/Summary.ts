// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ActivityStatus } from "./ActivityStatus";
import type { Event } from "./Event";
import type { Job } from "./Job";
import type { NextCandidate } from "./NextCandidate";
import type { RepoBrief } from "./RepoBrief";
import type { SchedulerState } from "./SchedulerState";

/**
 * GET /api/summary — polled by the UI every 5s and structurally checked
 * by `isSummary` (`hunter/ui-svelte/src/lib/validate.ts`): only the fields
 * a component dereferences without `?.`. A violation there blanks the
 * dashboard; anything else missing or renamed degrades silently, so this
 * shape is held by the Rust tests, not by the client.
 */
export type Summary = { backend_status_html: string, 
/**
 * All `FindingStatus` keys, zero-filled.
 */
counts: { [key in string]: number }, 
/**
 * Only observed types; may be {}.
 */
type_counts: { [key in string]: number }, repos: Array<RepoBrief>, last_cycle: Event | null, cycle_running: boolean, scheduler_paused: boolean, current_job: Job | null, next_candidate: NextCandidate | null, scheduler_state: SchedulerState | null, activity_status: ActivityStatus, };
